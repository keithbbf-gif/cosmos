# ARCH_SPEC 2026-08-17
# Merge for fan-out / critique. Not a live build. Not a novel.

**Written:** 2026-08-17 22:01 CT America/Chicago; wording fix 22:02 CT (two independent builds, not copies)
**Author of this merge:** GbQA / Cursor executor (Keith BBF, QA Engineer)
**Intended twins (KC-PC):** `V:\Ai\tmp\ARCH_SPEC_2026-08-17.md` and `C:\RECOVER_2026-08-17\ARCH_SPEC_2026-08-17.md`
**This write:** box only. **HOST DOWN** at 22:00 CT -- ExternalShell to KC-PC failed. Dest twins and family fire pending reconnect. Do not treat this box file as a live-tree write.

---

## 0. Authorship

| Who | What they wrote | What they did not |
|---|---|---|
| **SGH** | `FAMILY_OUT\SGH\ARCHITECT.md` Pass B (20:55 CT). Shape + adapt table. Richest host architect. | The plan. A live build ticket. |
| **OA** | `FAMILY_OUT\OA\ARCHITECT.md` confirm (Pass B after GROK). Gates before implementation. Independent check. | A rebuild architecture. |
| **This file** | GbQA merge of SGH shape + OA gates + writers table + live floor + OWNER lift-shape **under critique**. Fan-out packet. | GEM did not write a spec. Cursor GHX is not this floor. Cursor PR7 is draft / 251-file GHX, not the 296 corpus. |

SGH is architect. OA is independent check. Keith: get a written spec and fan it out for agreement/critique.

GEM ARCHITECT was a slogan block (thin; not a peer grok; not a vote). Cursor cloud GHX grind is a later writer on PRs, not the author of this floor.

---

## 1. Shape (SGH words; default until critique moves it)

KMesh is **one workstation, many minds, one pen**. Software lives in `V:\Ai\mesh-repo\BTS_MESH` + `ROLD`. BFast config lives in `V:\Ai\BFast`. Shared coordination lives on `V:\Ai` (letters, board, queue, lock). Dissertation/archive is `V:\Research4`. Public mirror is ITC. WAN rendezvous is GDX.

- **CoW is keeper, not the bus.** Nodes talk to nodes and to surfaces. Lanes (API/CLI/MCP/DOM/AGENT) are how a request reaches a model, not extra nodes.
- **Vertex = GEM rail**, Joanna.bbf read/use-only. Never register / upgrade / Activate. `keith.bbf@gmail.com` is GEM paid API + GDX. Do not collapse accounts.
- **`V:\A` HOLD** -- SGH §0 and OA Boundaries. Hashed briefs do not authorize a live write there. OWNER lift-shape is §3 (new mesh as a **separate effort** at `V:\A\Ai\`, not a cutover/copy of `V:\Ai`); it does not silently override this HOLD.
- **Adapt hashed modules.** Prefer existing bodies (`bts_paths`, `tree_lock`, `bts_board`, rails, governors). Gaps with missing bodies stay UNKNOWN.
- **Build nothing this packet.** SGH §4. OA: no code because gates are unresolved.

GHX and mesh-repo are **snapshots, not the pen**.

---

## 2. OA gates -- before any future implementation

From OA ARCHITECT. Unresolved. This spec does not close them.

1. OWNER resolves source promotion and supplies valid bodies/OCR for unresolved PDFs where needed.
2. Recover or formally retire `bts_fs_mcp.py`; do not substitute `bts_tools.py` for it.
3. Choose the canonical `00_TOOLS_INDEX.md` generator target and obtain a read-only `--check` drift measurement.
4. Repair/replace stale task action paths through an authorized change process; then measure behavior, not scheduler paint.
5. Establish a verifiable current lock/claim mechanism and eliminate the two-universe path ambiguity.
6. Separately authorize any current rail measurement. Treat any unmeasured lane as UNKNOWN.

No xerox/rebuild from stale or incomplete material.

---

## 3. OWNER direction under critique (not a live reconstruct)

Keith 22:00 CT incline, 22:01 CT correction -- lock as the **working shape to critique**, not as a HOLD lift and not as tonight's write.

SGH/OA said **`V:\A` HOLD**. This section is the **lift-shape** they must AGREE / DISAGREE / UNKNOWN.

**These are SEPARATE EFFORTS, not copies.** `V:\Ai` prior reconstruct and `V:\A\Ai` new mesh are two independent builds. Do not describe, design, or implement them as a clone, xerox, or copy of one onto the other. Neither tree is the source image of the other.

| Effort | Tree | Meaning |
|---|---|---|
| **PRIOR mesh / BTS** (independent reconstruct) | `V:\Ai\` | Outcome/intent: restore the prior workstation mesh here. **Not** TidyUP2 xerox. **Not** a copy of `V:\A\Ai`. **Not** two-universe Research4 baked in. |
| **NEW mesh** (independent build) | `V:\A\Ai\` | Already the dest for the new mesh. **Not** a copy of `V:\Ai`. **Not** a fourth silent-success universe. **Not** Research4-ROOT `bts_paths` transplanted. |
| **Legal** | stays `V:\Ai\Legal` | Keith-owned. No CoW on Legal. No family ingest. No Crucible sit from this spec. |
| **Isolation** | separate lock / phone / **queue** | Two independent builds. Separate `tree_lock.json`, separate `bts_phone` mailbox, separate queue. No shared lock, phone, queue, or overlay across `V:\Ai\` and `V:\A\Ai\`. |

**Reconstruct = outcome/intent of the prior effort only.** Do not xerox TidyUP2. Do not bake `bts_paths.ROOT=V:\Research4` into either effort. Do not copy prior onto new, or new onto prior.

**This spec does not reconstruct `V:\Ai` live and does not write `V:\A`.** Families critique the two-effort shape. GbPL does not start either build from this packet.

---

## 4. Writers (who holds the pen)

| piece | writer | reviewer | must not |
|---|---|---|---|
| BTS / paths / lock / runner / scheduled actions | GbPL Spine | CodeCREW Review | GbQA grind, CoW Legal, Crucible-as-author |
| tools index / tools_sync | GbPL (pen) | CodeCREW | `--write` before OWNER names path |
| COS (corrections.toml + corrections.py) | GbPL | COS Observer (audit only) | COS Observer writing |
| GHX / PRs | Cursor cloud | CodeCREW + GbQA | merge, #3 quarantine |
| Crucible sit | Keith later | -- | not a mesh writer |

GbQA orchestrates. COS Observer audits only. CoW never on Legal. Crucible sits later on the mesh; it does not write the mesh. Do not sit Crucible from this spec.

---

## 5. D3 write-path -- CONTESTED (default ADAPT)

`FAMILY_DISAGREE` D3 left unpicked. Fit-gap default: **ADAPT**.

| Position | Who | Status in this spec |
|---|---|---|
| Keep fail-loud dual resolve (`bts_paths` ROOT vs AIROOT). Do not invent a third tree. Build nothing this packet. | SGH | **DEFAULT / SPEC** |
| Adapt runner/lock/paths; build later a thin evidence-receipt adapter only | OA | **aligned with default** (gates first) |
| Add `coderoot()` / `live_mesh()` as a third role | GBW | **CRITIQUE TO ANSWER -- not the spec** |
| Build one durable write API over share-root roles | Cursor PR7 | **CRITIQUE TO ANSWER -- not the spec** |
| Slogan "one write path" | GEM | not a vote |

Do not invent a Cursor write API. Do not invent GBW `coderoot()` as the spec. Both remain contested options for family critique.

OWNER §3 (prior=`V:\Ai\`, new=`V:\A\Ai\`) is **two independent builds**, not a copy, not a silent-success fourth universe, and not a `coderoot()` implementation. If a family thinks §3 requires `coderoot()`, a new write API, or copying one tree onto the other, mark DISAGREE/UNKNOWN and say why. Do not implement.

---

## 6. Adapt now (SGH table, compressed)

Use as-is. Later mutation (not this packet) may change **where** they launch from.

- `bts_paths.py` `f5e94a5a…718f` -- fail-loud dual resolve. Any new path goes through it. No `D:\Research3` fallback.
- `tree_lock.py` `9ff0acfd…220c` -- cooperative claim/verify/release + manifest. Not enforcement. Do not mint `tree_lock.json`.
- `bts_board.py` -- CONTESTED = both positions, no third-model tiebreak.
- `bts_phone.py` -- V:\Ai letters. `--check` reports DEAD. Do not reopen Research4 as mailbox.
- `bts_vertex.py` / `bts_gem.py` -- GEM rail vs Studio wrapper. Keep separate. Never Activate.
- `bts_oa.py` + `bts_oa_api.py` -- two ledgers, two ceilings (272k vs 1.05M).
- `bts_gw.py` -- build rail; shares SGH $10 ceiling; unpriced ≠ $0. Not a second SGH grok.
- `bts_runner.py` -- claim-by-rename; RC 0/2/other. Scheduled **action path** was stale; module in mesh-repo is not.
- `tools_sync.py` -- `--check` then `--write`. No `--write` until OWNER names the live index path.
- `publish_nightly.bat` -- log outside published roots. Do not publish this phase.

Scheduled-task retarget (later): point leftover Research4 actions at mesh-repo copies, or one `bts_paths`-resolved launcher. Do not invent a third tree.

---

## 7. Live floor (KC-PC, measured 21:52–21:54 CT). Not invented.

From `MESH_FLOOR_FIX_2026-08-17.md` + `FIT_GAP_2026-08-17.md`.

| Fact | State |
|---|---|
| `RAIL_CHECK.bat` MESH | **now mesh-repo** (`set MESH=V:\Ai\mesh-repo\BTS_MESH`). Bat not fired tonight. Next schedule 2026-08-18 02:05 CT. |
| Drive Health | still `V:\Research4\Ai\BTS_MESH\bts_drive_health.py` (script missing). Last Result **2**. **Needs elevation.** Unelevated change blocked. |
| `\R2 Publish Nightly` | **Disabled**. Do not publish. |
| `tree_lock.json` | **MISSING**. `TREE IS FREE`. Do not mint `{}`. |
| `00_TOOLS_INDEX.md` | **absent**. Preview leftover in tmp is not the live target. |
| BFast | `--where` FATAL. `bts_fs_mcp.py` unique loss. **ACCEPT.** Do not rewrite a second server. |
| KDash Feed / Queue Runner | already mesh-repo. Last Result 0. |
| `bts_paths` | still ROOT=`V:\Research4`, AIROOT=`V:\Ai`, `mesh()` Research4. Two-universe leftover. Accepted tonight; not closed. |
| GDX host token | expired 2026-08-17T08:05Z. MCP + DriveFS live. No reauth from this spec. |
| Joanna Vertex | PONG on `project-5a33f910-1251-4d6a-bf9`. Balance UNKNOWN. No creditprobe. No Activate. |

---

## 8. What this spec authorizes

**Critique only.**

AGREE / DISAGREE / UNKNOWN per section, max 40 lines, one short generation each (OA / SGH / GBW). Cursor packet only unless a later GbQA send attaches this spec as the only ask.

This spec does **not** authorize:

- live reconstruct of `V:\Ai` (prior effort; not authorized tonight)
- any write to `V:\A` / `V:\A\Ai` (new mesh is a separate effort; HOLD stands; §3 is critique of the lift-shape, not a copy ticket)
- Crucible sit
- Joanna Activate / register / upgrade / attach / creditprobe
- TidyUP / TidyUP2 / TU2
- `claude.exe` / CoW Legal feed
- PR merge (PR7 stays draft; #3 quarantine)
- R2 publish / GrokDex regen
- `tree_lock.json` mint
- `tools_sync --write`
- inventing a Cursor write API
- inventing GBW `coderoot()` as the spec
- GEM vote / Joanna credit spend for this fan-out

---

## 9. Fan-out contract (for when host is up)

Packets: `FAMILY_OUT\<SGH|OA|GBW|CURSOR>\SPEC_IN.md` and `CREW\IN\2026-08-17_ARCH_SPEC.md`.

Fire spend-aware critique on **OA, SGH, GBW only**. Do not fire GEM. Do not start claude.exe. Do not launch Cursor cloud unless the spec is the only ask. Do not message sibling Grok Bot agents.

Prompt: Read ARCH_SPEC. You already grokked the corpus. Do NOT re-grok 296 files. Return AGREE / DISAGREE / UNKNOWN per section, max 40 lines. Write `FAMILY_OUT\<FAM>\SPEC_CRITIQUE.md`. Safety: no Legal, no V:\A write, no TidyUP, no secrets, no billing, no publish, no merge.

HOST DOWN 22:00 CT: packets staged on the box only. Families **not fired**.
